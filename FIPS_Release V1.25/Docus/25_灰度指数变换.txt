灰度指数变换

指数变换的基本表达式为：g(x,y) = c[b * (f(x,y) + a)] - 1  ([]代表指数)

参数a为指数偏移量，控制曲线的左右位置，范围：[-100, 100]。

参数b为指数加权量，用来控制曲线形状，范围：[-0.1, 10.0]

参数c为底数，也用来控制曲线形状，范围：[1.0, 10.0]

指数变换的作用是扩展图像的高灰度级、压缩低灰度级，和对数变换的效果相反。

虽然幂次变换也有这个功能，但是图像经过指数变换后对比度更高，高灰度级也被扩展到了更宽的范围。